### Rename to .env and fill with real credentials (never commit .env)

# -------------------------------------------------
# Frontend Firebase (Vite will expose only vars prefixed with VITE_)
# -------------------------------------------------
VITE_FIREBASE_API_KEY=your_firebase_web_api_key
VITE_FIREBASE_AUTH_DOMAIN=your-app.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-firebase-project-id
VITE_FIREBASE_APP_ID=1:xxxxxxxxxxxx:web:yyyyyyyyyyyyyy
VITE_FIREBASE_STORAGE_BUCKET=your-app.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=1234567890

# -------------------------------------------------
# Backend Firebase Admin (Service Account)
# Obtain from Firebase Console > Project Settings > Service Accounts
# PRIVATE KEY: paste exactly; if adding to Vercel UI you can keep real newlines.
# -------------------------------------------------
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-firebase-project-id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nLINE1\nLINE2...\n-----END PRIVATE KEY-----\n"

# -------------------------------------------------
# Doctor Dashboard Auth
# Credentials used for /api/auth/login (server side only)
# -------------------------------------------------
DOCTOR_EMAIL=doctor@example.com
DOCTOR_PASSWORD=change_this_strong_password

# HMAC signing secret for doctor token (rotate to invalidate all sessions)
JWT_SECRET=generate_a_long_random_string_here

# -------------------------------------------------
# Core server
# -------------------------------------------------
PORT=5174

# -------------------------------------------------
# SMTP configuration (optional – leave blank to disable email)
# -------------------------------------------------
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password_or_app_password

# Optional overrides
MAIL_FROM="Clinic <no-reply@example.com>"
MAIL_TO=admin@example.com
MAIL_ACK=true

# Notes:
# - Set MAIL_ACK=false to disable patient acknowledgment email.
# - For Gmail, enable 2FA and create an App Password.
# - Never commit the actual .env file with real secrets.
# - Do NOT prefix backend-only secrets with VITE_.

# -------------------------------------------------
# Feature flags
# -------------------------------------------------
# When 'true', new signups require doctor approval before login.
# When 'false', signup grants immediate access (approval code is present but inactive).
VITE_REQUIRE_APPROVAL=false